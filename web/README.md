# Fractal wgpu wasm binding

A Rust crate rendering a fractal using Web GPU. Inteded to be run in the browser using WASM

## Build

From workspace root:

```shell
wasm-pack build web --target web
```
